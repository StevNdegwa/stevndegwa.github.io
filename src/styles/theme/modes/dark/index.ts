import { colors } from "../../colors";

export const dark = {
    global: {
        bg: colors.dark,
        color: colors.light
    },
    actions: {
        bg: colors.primary[6],
        color: colors.light
    },
    icons: {
        color: colors.primary[3]
    },
    highlight: {
        bg: colors.dark,
        color: colors.light
    },
    card: {
        bg: colors.light,
        color: colors.primary[2]
    }
}
