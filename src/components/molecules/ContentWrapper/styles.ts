import styled from "styled-components";
import { FlexBox } from "../FlexBox";

export const Wrapper = styled(FlexBox)`
  max-width: 1024px;
  margin: auto;
  padding-left: 1rem;
  padding-right: 1rem;
`;
