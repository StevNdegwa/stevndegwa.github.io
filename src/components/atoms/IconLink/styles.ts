import styled from "styled-components";
import { Link } from "gatsby";

export const LinkWrapper = styled(Link)`
padding:0.4rem;
background-color:transparent;
border:none;
font-size:1.5rem;
color:hsl(145, 61%, 59%, 1);
`;